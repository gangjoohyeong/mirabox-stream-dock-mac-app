# DESIGN.md

이 레포에서 UI를 만들 때의 규칙이다. 값은 `tokens.css`에 있고, 이 문서는 그 값을
언제 어떻게 쓰는지와 **왜 그런지**를 담는다. 이유를 적어둔 것은, 예외 요청이 들어왔을 때
규칙을 버리는 대신 규칙 안에서 해결하기 위해서다.

## 이 앱이 무엇인가

macOS 데스크톱 앱이다. 브라우저에서 도는 웹사이트가 아니다.
시각적 기준은 Linear의 **제품 UI**다. Linear의 마케팅 사이트가 아니다.

이 구분이 중요하다. "Linear 스타일"이라는 말은 인터넷에서 대체로
다크 배경 + 그라디언트 + 글로우 + 스크롤 애니메이션이 붙은 랜딩페이지 트렌드를 가리킨다.
그건 이 앱이 참고하는 대상이 아니다. 참고하는 것은 이슈 목록이 빽빽하게 들어찬,
조용하고 밀도 높은 작업 도구 화면이다.

랜딩페이지 관습(히어로 섹션, 큰 그림자, 그라디언트, 등장 애니메이션)을 가져오지 마라.

## 위계를 만드는 법

이 순서대로 시도한다. 앞의 방법으로 해결되면 뒤로 가지 않는다.

1. **표면 사다리** — `--surface-0` ~ `--surface-3`. 배경 명도로 앞뒤를 만든다
2. **여백** — 관련 있는 것끼리 붙이고 그룹 사이를 `--space-6` 이상 띄운다
3. **텍스트 농도** — `--fg` / `--fg-secondary` / `--fg-tertiary` 세 단계
4. **헤어라인 테두리** — `0.5px solid var(--border)`. 1px 은 쓰지 않는다
5. **굵기** — `--w-medium`(510)까지. 마지막 수단이다

**그림자는 쓰지 않는다.** 표면 사다리가 그 일을 대신한다. 모달과 커맨드 팔레트조차
`--surface-3` + 테두리만으로 떠 있게 만든다. 그림자를 넣는 순간 웹 대시보드가 된다.

테두리를 0.5px로 두는 이유: 레티나에서 실제로 반 픽셀로 그려져 훨씬 정밀해 보인다.
1px 테두리는 이 밀도에서 격자처럼 시끄러워진다.

## 타이포

굵기는 **400 / 510 / 590 세 단계만** 쓴다. 700을 쓰지 않는 이유는, 볼드가 없으면
화면이 조용해지고 그만큼 밀도를 높여도 시끄럽지 않기 때문이다. 목록 행 높이를
34px까지 줄일 수 있는 것이 이 덕분이다. 강조가 필요하면 590이 아니라
표면 단계나 여백을 먼저 검토한다.

510은 regular와 medium 사이의 값으로, 소리치지 않는 강조를 만든다. UI 텍스트의
사실상 기본값으로 쓴다.

`font-feature-settings`의 `cv01` / `ss03` / `zero`를 지우지 마라. 이 셋이 Inter를
범용 폰트가 아닌 이 앱의 서체로 만든다. 특히 ID나 숫자에는 `--font-mono`와
슬래시 제로를 함께 써서 기술 도구의 인상을 유지한다.

자간은 `--tracking`(-0.011em)이 기본이고, 15px 이상 제목에만 `--tracking-title`.
본문에서 자간을 임의로 조정하지 마라.

## 색

거의 완전한 무채색이다. 색은 기능에만 쓰고 장식에는 쓰지 않는다.

- **`--accent`**: 뷰당 기본 동작 **하나**와 포커스 링, 선택 표시. 그 외에는 쓰지 않는다.
  보조 버튼, 링크 강조, 아이콘 채색에 쓰지 마라.
- **`--status-*`**: 상태 아이콘 글리프에만. 텍스트 색이나 배경으로 쓰지 마라.
- 라벨의 색 점은 6px 원으로만 표현한다. 라벨 배경을 색으로 칠하지 마라.

액센트를 늘리고 싶어지면, 그건 위계가 여백으로 안 풀렸다는 신호다. 3번으로 돌아가라.

## 밀도

- 목록 행 높이 34px, 패딩 `0 var(--space-4)`
- 사이드바 항목 패딩 `5px var(--space-2)`
- 4px 그리드를 벗어나는 값을 만들지 마라

밀도가 높은 대신 조용해야 한다. 밀도와 장식을 동시에 올리면 읽을 수 없어진다.

## 모션

- `--duration-fast` / `--duration-base`만. 140ms를 넘기지 않는다
- 이징은 `--ease-out`
- 사용자의 동작에 대한 응답일 때만. 등장 fade-up, 무한 루프, 스크롤 트리거는 금지
- `prefers-reduced-motion`은 `tokens.css`에서 처리된다

## 키보드

**이 앱의 정체성은 시각보다 키보드에 있다.** 마우스 없이 주요 작업이 끝나야 한다.

- `⌘K` 커맨드 팔레트에서 모든 동작에 접근 가능
- 목록에서 `J`/`K`와 화살표로 이동, `Enter`로 열기, `Esc`로 닫기
- 자주 쓰는 동작은 단일 키 단축키 (상태 `S`, 담당 `A`, 라벨 `L`)
- 단축키를 UI에 노출한다. 팔레트 항목 우측, 툴팁, 메뉴에 표기
- 새 기능을 만들 때 단축키 없이 마우스 전용으로 두지 마라

## macOS 통합

- **타이틀바**: `titleBarStyle: 'hiddenInset'`. 좌측 콘텐츠는 `--traffic-light-inset`만큼 민다
- **드래그**: 상단 바에 `.drag-region`, 그 안의 버튼·입력에 `.no-drag`.
  빠뜨리면 창이 안 움직이거나 버튼이 안 눌린다
- **비활성 창**: main에서 `win.on('blur'|'focus')`로 `body.window-inactive` 토글
- **다크 모드**: `prefers-color-scheme`만 쓴다. 앱 안에 테마 토글을 만들지 마라.
  `nativeTheme`을 강제하지 마라
- **메뉴**: Electron 네이티브 `Menu`. HTML로 흉내내지 마라
- **텍스트 선택**: 앱 크롬은 선택 불가가 기본. 사용자 콘텐츠에만 `.selectable`

## 컴포넌트

shadcn/ui를 쓴다. 컴포넌트 코드가 프로젝트로 복사되므로 직접 고칠 수 있고,
그게 이 토큰에 맞춰 다듬기에 가장 낫다.

- 베이스(Base UI / Radix / React Aria)는 프로젝트 초기화 때 정한 것을 끝까지 유지한다
- 드롭다운, 다이얼로그, 툴팁, 콤보박스, 커맨드 팔레트는 직접 만들지 말고 가져다 쓴다.
  포커스 트랩·키보드 내비게이션·스크린리더 처리가 이미 되어 있다
- 가져온 뒤 반드시 `tokens.css` 변수로 색·간격·반경을 다시 매핑한다.
  기본 테마를 그대로 두면 웹 대시보드처럼 보인다

## 화면의 네 가지 상태

모든 화면이 아래를 갖춰야 완성이다.

- **빈 상태** — 무엇을 하면 되는지 알려준다. 일러스트를 넣지 마라
- **로딩** — 스피너보다 스켈레톤. 200ms 미만이면 아무것도 보여주지 않는다
- **에러** — 무엇이 잘못됐고 무엇을 하면 되는지. 사과하지 않는다
- **정상**

## 문구

- 버튼은 눌렀을 때 일어나는 일을 그대로 쓴다. "확인"이 아니라 "삭제"
- 같은 동작은 흐름 전체에서 같은 단어를 쓴다
- 문장형 대소문자. 마침표는 완전한 문장에만
- 시스템 용어가 아니라 사용자 용어로 쓴다

## 하지 말 것

이유가 붙은 것은 이유까지 읽어라. 대안이 그 안에 있다.

- **그림자** → 표면 사다리를 쓴다
- **그라디언트** (배경, 버튼, 텍스트, 테두리, 어디에도)
- **700 굵기** → 590까지. 그전에 여백과 표면을 검토한다
- **1px 이상 테두리, 유채색 테두리** → 0.5px `--border`
- **액센트 컬러의 장식적 사용** → 뷰당 기본 동작 하나에만
- **토큰에 없는 색·간격·반경 값** → 필요하면 만들지 말고 물어본다
- **이모지를 아이콘으로 쓰기** → 단색 라인 SVG
- **ALL CAPS 라벨, 자간을 벌린 eyebrow 텍스트**
- **앱 내 테마 토글** → 시스템을 따른다
- **사용자가 유발하지 않은 애니메이션**
- **`!important`, 인라인 style로 색·간격 지정**
- **커스텀 스크롤바를 두껍게 만들기**

## 예외 요청이 들어왔을 때

"여기를 더 눈에 띄게 해줘" 같은 요청에 그림자나 볼드로 대응하지 마라.
아래 순서로 시도하고, 셋 다 안 되면 그때 물어본다.

1. 주변 여백을 늘려 고립시킨다
2. 표면 단계를 한 칸 올린다
3. 텍스트 농도를 한 단계 올리거나 굵기를 510으로 올린다

## 참고

시각적 기준은 `reference.html`이다. 마크업을 그대로 베끼지 말고 밀도와 톤만 참고한다.
새 화면을 만들 때는 이 문서와 `tokens.css`를 읽고, 필요하면 `reference.html`의
스크린샷을 첨부받아 작업한다.
